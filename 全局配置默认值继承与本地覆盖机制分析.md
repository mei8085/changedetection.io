# changedetection.io 全局配置默认值继承与本地覆盖机制分析（修正版）

## 一、整体架构概述

changedetection.io 采用 **多级配置继承架构**，但不同字段的继承方式差异很大，并非统一的三级级联：

```
Watch 级别（监控条目）
    ↓ 依字段类型不同而异
Tag 级别（标签/分组）
    ↓
Global Settings（全局配置）
    ↓
代码内置默认值
```

**关键认知**：当前实现为 **手动散点式解析**，各字段的继承/覆盖逻辑分布在不同模块中，且机制各不相同——有的采用 None→Global 回退、有的采用开关控制、有的采用 OR 逻辑、有的采用列表合并。不可一概而论。

---

## 二、新增监控条目时的默认值初始化流程

### 2.1 入口

所有 Watch 对象的字段默认值在 [watch_base.__init__()](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/__init__.py#L158-L308) 中通过 `self.update({...})` 统一设置。

### 2.2 "未设置"的标记值

| 标记值 | 字段 | 含义 |
|--------|------|------|
| `None` | `strip_ignored_lines`、`use_page_title_in_list`、`webdriver_delay`、`proxy`、`history_snapshot_max_length`、`notification_title`、`notification_body` 等 | 使用全局配置中的对应值 |
| `'system'` | `fetch_backend` | 使用全局配置的 fetch_backend |
| `'System default'` | `notification_format` | 使用全局配置的通知格式 |
| `time_between_check_use_default=True` | 调度相关 | 时间间隔和时间窗口均使用全局配置 |
| `False` | `notification_muted` | 默认不静默（注意：不是 None） |

> **源码注释**（[model/__init__.py#L176-L178](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/__init__.py#L176-L178)）："Re #110, so then if this is set to None, we know to use the default value instead"

---

## 三、字段分类详解：继承方式与本地覆盖

### 3.1 页面标题显示开关（use_page_title_in_list）—— OR 逻辑，非级联回退

> ⚠️ **修正**：此前错误地将其描述为"三态布尔 Watch → Global 级联回退"。实际运行时逻辑为 **OR 取并集**，且全局配置路径不同。

#### 全局配置路径

```
settings.application.ui.use_page_title_in_list    ← 注意：嵌套在 ui 子字典下
```

源码：[model/App.py#L78](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/App.py#L78)

```python
'ui': {
    'use_page_title_in_list': True,
    ...
}
```

#### 运行时解析逻辑

源码：[watch-overview.html#L244-L298](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/watchlist/templates/watch-overview.html#L244-L298)

```html
{%- set system_use_url_watchlist = datastore.data['settings']['application']['ui'].get('use_page_title_in_list')  -%}
...
{% if system_use_url_watchlist or watch.get('use_page_title_in_list') %}
    {{ watch.label }}
{% else %}
    {{ watch.get('title') or watch.link }}
{% endif %}
```

**这是 OR 逻辑**：只要全局或 Watch 本地任意一方为 True，就显示页面标题。

#### 行为矩阵

| 全局 `ui.use_page_title_in_list` | Watch `use_page_title_in_list` | 实际效果 |
|:---:|:---:|:---:|
| True | None | ✅ 显示标题 |
| True | True | ✅ 显示标题 |
| True | **False** | ✅ **仍然显示**（全局覆盖了本地） |
| False | None | ❌ 不显示 |
| False | True | ✅ 显示 |
| False | False | ❌ 不显示 |

**结论**：Watch 的 `use_page_title_in_list` 虽然使用了 `TernaryNoneBooleanField`，但在模板渲染时并非级联回退——当全局开关打开时，Watch 无法通过本地设置关闭标题显示。只有在全局关闭时，Watch 才能本地开启。

#### 全局设置表单

源码：[forms.py#L1040](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/forms.py#L1040)

```python
class globalSettingsApplicationUIForm(Form):
    use_page_title_in_list = BooleanField(_l('Use page <title> in watch overview list'))
```

这是一个普通 `BooleanField`，位于 `globalSettingsApplicationUIForm`（UI 子表单），表单前缀为 `application-ui-`。

---

### 3.2 移除元素选择器（subtractive_selectors）—— Tag → Watch → Global 顺序合并

> ⚠️ **修正**：此前描述为"Watch + Tag + Global 合并"，实际叠加顺序为 **Tag → Watch → Global**，且此字段不使用去重。

#### 运行时解析逻辑

源码：[processors/text_json_diff/processor.py#L79-L86](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py#L79-L86)

```python
@property
def subtractive_selectors(self):
    if self._subtractive_selectors_cache is None:
        watch_selectors = self.watch.get("subtractive_selectors", [])
        tag_selectors = self.datastore.get_tag_overrides_for_watch(uuid=self.watch_uuid, attr='subtractive_selectors')
        global_selectors = self.datastore.data["settings"]["application"].get("global_subtractive_selectors", [])
        self._subtractive_selectors_cache = [*tag_selectors, *watch_selectors, *global_selectors]
    return self._subtractive_selectors_cache
```

**叠加顺序**：`Tag → Watch → Global`

**与通用合并方法 `_get_merged_rules()` 的差异**：

| 特性 | `subtractive_selectors`（专用属性） | `_get_merged_rules()`（通用方法） |
|------|:---:|:---:|
| 叠加顺序 | Tag → Watch → Global | Watch → Tag（然后可选 Global） |
| 去重 | ❌ 不去重，允许重复 | ✅ `dict.fromkeys()` 去重 |
| 包含全局 | ✅ 始终包含 `global_subtractive_selectors` | 需 `include_global=True` 参数 |

**影响**：CSS 选择器的移除顺序可能影响页面处理结果（先移除 Tag 指定的元素 → 再移除 Watch 指定的元素 → 最后移除全局指定的元素），且重复选择器不会自动合并。

#### 其他过滤器字段的通用合并逻辑

源码：[processors/text_json_diff/processor.py#L57-L67](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py#L57-L67)

```python
def _get_merged_rules(self, attr, include_global=False):
    watch_rules = self.watch.get(attr, [])
    tag_rules = self.datastore.get_tag_overrides_for_watch(uuid=self.watch_uuid, attr=attr)
    rules = list(dict.fromkeys(watch_rules + tag_rules))  # Watch + Tag，去重
    if include_global:
        global_rules = self.datastore.data['settings']['application'].get(f'global_{attr}', [])
        rules = list(dict.fromkeys(rules + global_rules))
    return rules
```

通用方法顺序为 **Watch → Tag → [Global]**，并使用 `dict.fromkeys()` 去重。

#### 各过滤器字段对比

| 字段名 | 合并方法 | Watch | Tag | Global | 全局配置 key | 去重 | 叠加顺序 |
|--------|---------|:-----:|:---:|:------:|-------------|:---:|---------|
| `subtractive_selectors` | 专用属性 | ✅ | ✅ | ✅ | `global_subtractive_selectors` | ❌ | Tag→Watch→Global |
| `ignore_text` | `_get_merged_rules` | ✅ | ✅ | ✅ | `global_ignore_text` | ✅ | Watch→Tag→Global |
| `include_filters` | `_get_merged_rules` | ✅ | ✅ | ❌ | - | ✅ | Watch→Tag |
| `extract_lines_containing` | `_get_merged_rules` | ✅ | ✅ | ❌ | - | ✅ | Watch→Tag |
| `extract_text` | `_get_merged_rules` | ✅ | ✅ | ❌ | - | ✅ | Watch→Tag |
| `trigger_text` | `_get_merged_rules` | ✅ | ✅ | ❌ | - | ✅ | Watch→Tag |
| `text_should_not_be_present` | `_get_merged_rules` | ✅ | ✅ | ❌ | - | ✅ | Watch→Tag |

---

### 3.3 调度时间窗口（time_schedule_limit）—— 与时间间隔共用开关

> ⚠️ **修正**：此前未明确说明 `time_schedule_limit` 与 `time_between_check` 共用同一个 `time_between_check_use_default` 开关，且全局 `time_schedule_limit` 存储位置不同。

#### 开关控制逻辑

源码：[flask_app.py#L1189-L1197](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/flask_app.py#L1189-L1197)

```python
if watch.get('time_between_check_use_default'):
    time_schedule_limit = datastore.data['settings']['requests'].get('time_schedule_limit', {})
    scheduler_source = 'system/global settings'
else:
    time_schedule_limit = watch.get('time_schedule_limit')
    scheduler_source = 'watch'
```

**`time_between_check_use_default` 控制两个配置的继承**：

| 开关值 | 检查间隔来源 | 时间窗口来源 |
|--------|-------------|-------------|
| `True`（默认） | 全局 `settings.requests.time_between_check` | 全局 `settings.requests.time_schedule_limit` |
| `False` | Watch 本地 `time_between_check` | Watch 本地 `time_schedule_limit` |

注意 `time_schedule_limit` 在全局存储于 **`settings.requests`** 下（不是 `settings.application` 下），这与大多数全局配置的位置不同。

#### time_schedule_limit 结构

源码：[forms.py#L189-L203](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/forms.py#L189-L203)

```python
class ScheduleLimitForm(Form):
    enabled = BooleanField("Use time scheduler", default=False)
    monday = FormField(ScheduleLimitDaySubForm)
    ...  # 每天一个子表单
    sunday = FormField(ScheduleLimitDaySubForm)
    timezone = StringField("Optional timezone to run in")  # ← 可选时区覆盖
```

**每个 `time_schedule_limit`（无论全局还是 Watch 本地）都自带一个 `timezone` 子字段**。

---

### 3.4 时区默认值（scheduler_timezone_default）—— 通过 schedule.timezone 间接覆盖

> ⚠️ **修正**：此前将 `scheduler_timezone_default` 列为"仅全局配置（不可本地覆盖）"。实际上 Watch 可以通过 `time_schedule_limit.timezone` 子字段间接覆盖时区。

#### 时区解析链

源码：[flask_app.py#L1199](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/flask_app.py#L1199)、[edit.py#L260-L262](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/ui/edit.py#L260-L262)、[time_handler.py#L96-L98](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/time_handler.py#L96-L98)

```python
tz_name = time_schedule_limit.get('timezone')            # 1. 当前生效的 schedule 中的 timezone
if not tz_name:
    tz_name = datastore.data['settings']['application'].get(
        'scheduler_timezone_default',                     # 2. 全局默认时区
        os.getenv('TZ', 'UTC').strip()                    # 3. 环境变量 TZ
    )                                                     # 4. 最终回退 'UTC'
```

**解析优先级**：

```
time_schedule_limit.timezone  →  settings.application.scheduler_timezone_default  →  env TZ  →  'UTC'
       (1)                                (2)                                      (3)        (4)
```

- 优先级 (1) 的来源取决于 `time_between_check_use_default`：
  - `True` 时 → 全局 `settings.requests.time_schedule_limit.timezone`
  - `False` 时 → Watch 本地 `time_schedule_limit.timezone`

#### Watch 级别覆盖时区的方式

Watch 并没有独立的 `scheduler_timezone_default` 字段，但可以在编辑表单的 Schedule 面板中填写 **timezone** 子字段来覆盖：

- 编辑表单中的 timezone placeholder 显示全局 `scheduler_timezone_default` 值（[forms.py#L967-L970](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/forms.py#L967-L970)）
- 用户填写后，值存入 `watch['time_schedule_limit']['timezone']`
- 运行时优先使用该值

---

### 3.5 三态布尔字段（TernaryNoneBooleanField）—— None→Global 回退

这类字段使用 `TernaryNoneBooleanField` 控件，支持三种状态：None（使用全局默认）、True（本地启用）、False（本地禁用）。

#### 适用字段与解析逻辑

| 字段名 | Watch 默认值 | 全局配置路径 | 解析方式 |
|--------|-------------|-------------|---------|
| `strip_ignored_lines` | `None` | `settings.application.strip_ignored_lines` | None→全局，True/False→本地 |
| `notification_muted` | `False` | 无全局对应字段 | 直接布尔判断 |

**注意**：`notification_muted` 虽然使用 `TernaryNoneBooleanField`，但 `watch_base` 初始化默认值为 `False`（不是 `None`），且无全局级对应字段。它在通知级联逻辑中作为**门控条件**使用，而非配置覆盖。

#### strip_ignored_lines 解析示例

源码：[processors/text_json_diff/processor.py#L587-L593](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py#L587-L593)

```python
strip_ignored_lines = watch.get('strip_ignored_lines')
if strip_ignored_lines is None:
    strip_ignored_lines = self.datastore.data['settings']['application'].get('strip_ignored_lines')
```

---

### 3.6 通知系统字段（三级级联继承）

由 [notification_service.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py) 的 `_check_cascading_vars()` 函数统一处理。

#### 解析优先级

```
Watch 本地值 → Tag 标签覆盖 → Global 全局值 → 代码内置默认
```

#### 适用字段

| 字段名 | 说明 | 特殊规则 |
|--------|------|---------|
| `notification_urls` | 通知 Apprise URL 列表 | `notification_muted=True` 时跳过 Watch 层 |
| `notification_title` | 通知标题模板 | - |
| `notification_body` | 通知内容模板 | - |
| `notification_format` | 通知格式 | Watch 值为 `'System default'` 时回退到全局 |

#### notification_muted 的门控作用

源码：[notification_service.py#L30-L40](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py#L30-L40)

```python
v = watch.get(var_name)
if v and not watch.get('notification_muted'):   # Watch 层：muted 时跳过
    ...
    return v

tags = datastore.get_all_tags_for_watch(uuid=watch.get('uuid'))
if tags:
    for tag_uuid, tag in tags.items():
        v = tag.get(var_name)
        if v and not tag.get('notification_muted'):  # Tag 层：muted 时跳过
            return v
```

当 Watch 或 Tag 的 `notification_muted=True` 时，该层级的通知配置被跳过，继续向下查找。此外还有全局级 `all_muted` 开关，由 [queue_handlers.py#L448](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/queue_handlers.py#L448) 在通知队列入队时检查，为 True 时完全阻止通知发送。

---

### 3.7 内容获取后端（fetch_backend）

源码：[processors/base.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py)

```python
prefer_fetch_backend = self.watch.get('fetch_backend', 'system')
if not prefer_fetch_backend or prefer_fetch_backend == 'system':
    prefer_fetch_backend = self.datastore.data['settings']['application'].get('fetch_backend')
```

- Watch 默认值 `'system'` → 回退到全局 `settings.application.fetch_backend`
- 用户可本地覆盖：选择 `'html_requests'`、`'playwright_chromium'` 等具体后端

---

### 3.8 其他 Watch → Global 回退字段

| 字段名 | Watch 默认值 | 全局配置路径 | 解析位置 |
|--------|-------------|-------------|---------|
| `proxy` | `None` | `settings.requests.proxy` | [store/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py) `get_preferred_proxy_for_watch()` |
| `webdriver_delay` | `None` | `settings.application.webdriver_delay` | [processors/base.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py) |
| `history_snapshot_max_length` | `None` | `settings.application.history_snapshot_max_length` | [model/Watch.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/Watch.py) `save_history_blob()` |

---

### 3.9 仅全局配置的字段（不可本地覆盖）

| 字段名 | 全局配置路径 | 说明 |
|--------|-------------|------|
| `timeout` | `settings.requests.timeout` | HTTP 请求超时时间 |
| `workers` | `settings.requests.workers` | 并发工作线程数 |
| `jitter_seconds` | `settings.requests.jitter_seconds` | 调度抖动秒数 |
| `default_ua` | `settings.requests.default_ua` | 默认 User-Agent |
| `ignore_whitespace` | `settings.application.ignore_whitespace` | 比较时忽略空白（注意：另有本地字段 `trim_text_whitespace` 用于截取首尾空白，两者功能不同） |
| `empty_pages_are_a_change` | `settings.application.empty_pages_are_a_change` | 空页面是否视为变更 |
| `all_paused` | `settings.application.all_paused` | 全局暂停所有检查 |
| `all_muted` | `settings.application.all_muted` | 全局静默所有通知 |

**注意**：`scheduler_timezone_default` 虽然自身仅存于全局配置，但 Watch 可以通过 `time_schedule_limit.timezone` 子字段间接覆盖（见 3.4 节），因此不应简单归类为"不可覆盖"。

---

## 四、继承方式分类总览

changedetection.io 中实际存在 **五种不同的继承/覆盖模式**：

### 模式 A：None 回退（Watch → Global）

Watch 字段默认为 `None`，运行时检查：若为 `None` 则读取全局值。

| 字段 | 标记值 |
|------|--------|
| `strip_ignored_lines` | `None` |
| `webdriver_delay` | `None` |
| `proxy` | `None` |
| `history_snapshot_max_length` | `None` |
| `notification_title` | `None` |
| `notification_body` | `None` |

### 模式 B：特殊值回退

Watch 字段使用特殊字符串标记"使用全局"，运行时判断该标记则回退。

| 字段 | 标记值 |
|------|--------|
| `fetch_backend` | `'system'` |
| `notification_format` | `'System default'` |

### 模式 C：开关控制

一个布尔开关控制 Watch 是使用全局配置还是本地配置。开关同时控制**检查间隔**和**时间窗口**两个配置。

| 开关字段 | 控制的配置 | 默认值 |
|---------|-----------|--------|
| `time_between_check_use_default` | `time_between_check` + `time_schedule_limit` | `True` |

### 模式 D：OR 逻辑（全局 OR 本地）

只要全局或本地任一为 True 即生效，Watch 无法在全局开启时本地关闭。

| 字段 | 全局路径 |
|------|---------|
| `use_page_title_in_list` | `settings.application.ui.use_page_title_in_list` |

### 模式 E：列表合并追加

多级规则列表合并，本地与全局规则**同时生效**，不互相覆盖。

| 字段 | 合并层级 | 顺序 | 去重 |
|------|---------|------|------|
| `subtractive_selectors` | Tag + Watch + Global | Tag→Watch→Global | ❌ |
| `ignore_text` | Watch + Tag + Global | Watch→Tag→Global | ✅ |
| `include_filters` | Watch + Tag | Watch→Tag | ✅ |
| `extract_lines_containing` | Watch + Tag | Watch→Tag | ✅ |
| `extract_text` | Watch + Tag | Watch→Tag | ✅ |
| `trigger_text` | Watch + Tag | Watch→Tag | ✅ |
| `text_should_not_be_present` | Watch + Tag | Watch→Tag | ✅ |

---

## 五、字段完整总览表

### 可本地覆盖的字段

| 字段名 | Watch 默认值 | 继承模式 | 全局配置路径 | 覆盖说明 |
|--------|-------------|---------|-------------|---------|
| `fetch_backend` | `'system'` | B: 特殊值回退 | `settings.application.fetch_backend` | `'system'` 时用全局 |
| `time_between_check` | 全 None dict | C: 开关控制 | `settings.requests.time_between_check` | 开关 True 时用全局 |
| `time_schedule_limit` | 全默认 dict | C: 开关控制 | `settings.requests.time_schedule_limit` | 开关 True 时用全局 |
| `time_between_check_use_default` | `True` | 开关本身 | - | 控制间隔和窗口 |
| `proxy` | `None` | A: None 回退 | `settings.requests.proxy` | None 时用全局 |
| `webdriver_delay` | `None` | A: None 回退 | `settings.application.webdriver_delay` | None 时用全局 |
| `history_snapshot_max_length` | `None` | A: None 回退 | `settings.application.history_snapshot_max_length` | None 时用全局 |
| `strip_ignored_lines` | `None` | A: None 回退 | `settings.application.strip_ignored_lines` | 三态布尔 |
| `use_page_title_in_list` | `None` | D: OR 逻辑 | `settings.application.ui.use_page_title_in_list` | 全局开时无法本地关 |
| `notification_muted` | `False` | 门控条件 | 无全局对应 | 通知级联中的门控 |
| `notification_urls` | `[]` | 三级级联 | `settings.application.notification_urls` | Watch→Tag→Global |
| `notification_title` | `None` | 三级级联 | `settings.application.notification_title` | Watch→Tag→Global |
| `notification_body` | `None` | 三级级联 | `settings.application.notification_body` | Watch→Tag→Global |
| `notification_format` | `'System default'` | B: 特殊值+级联 | `settings.application.notification_format` | 特殊值触发回退 |
| `include_filters` | `[]` | E: 列表合并 | - | Watch+Tag 合并去重 |
| `subtractive_selectors` | `[]` | E: 列表合并 | `global_subtractive_selectors` | Tag→Watch→Global 不去重 |
| `ignore_text` | `[]` | E: 列表合并 | `global_ignore_text` | Watch→Tag→Global 合并去重 |
| `extract_lines_containing` | `[]` | E: 列表合并 | - | Watch+Tag 合并去重 |
| `extract_text` | `[]` | E: 列表合并 | - | Watch+Tag 合并去重 |
| `trigger_text` | `[]` | E: 列表合并 | - | Watch+Tag 合并去重 |
| `text_should_not_be_present` | `[]` | E: 列表合并 | - | Watch+Tag 合并去重 |
| `time_schedule_limit.timezone` | 空 | 间接覆盖 | `settings.application.scheduler_timezone_default` | 通过 schedule 子字段覆盖全局时区 |

### 仅全局配置的字段

| 字段名 | 全局配置路径 |
|--------|-------------|
| `timeout` | `settings.requests.timeout` |
| `workers` | `settings.requests.workers` |
| `jitter_seconds` | `settings.requests.jitter_seconds` |
| `default_ua` | `settings.requests.default_ua` |
| `ignore_whitespace` | `settings.application.ignore_whitespace` |
| `empty_pages_are_a_change` | `settings.application.empty_pages_are_a_change` |
| `all_paused` | `settings.application.all_paused` |
| `all_muted` | `settings.application.all_muted` |

---

## 六、边界情况详解

### 6.1 代理选择决策（proxy）—— 四级回退链 + no-proxy 特殊值

> **`proxy` 字段虽然在总览表中被归类为模式 A（None 回退），但实际决策过程远比简单的 Watch→Global 复杂，涉及环境变量、无效配置校验和默认回退。**

#### 代理列表构建

源码：[store/__init__.py#L813-L853](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py#L813-L853)

代理列表的来源：

| 来源 | 条件 | Key 命名 |
|------|------|---------|
| `proxies.json` 外部文件 | 文件存在 | 文件中定义的 key |
| `settings.requests.extra_proxies` UI 配置 | 存在且含 proxy_name + proxy_url | `"ui-" + i + proxy_name` |
| 内置 "No proxy" 选项 | `ENABLE_NO_PROXY_OPTION` 环境变量为 True（默认） | `"no-proxy"` |

**"No proxy" 选项的特殊性**：`proxy_list["no-proxy"] = {'label': "No proxy", 'url': ''}`，其 `url` 为空字符串，表示直连不走代理。

#### 代理选择决策链

源码：[store/__init__.py#L855-L884](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py#L855-L884)

```python
def get_preferred_proxy_for_watch(self, uuid):
    # 前置：若无任何代理配置，直接返回 None（即无代理）
    if self.proxy_list is None:
        return None

    watch = self.data['watching'].get(uuid)

    # 优先级 1：Watch 指定 "no-proxy" → 强制无代理
    if strtobool(os.getenv('ENABLE_NO_PROXY_OPTION', 'True')) and watch.get('proxy') == "no-proxy":
        return None

    # 优先级 2：Watch 指定了有效代理 key → 使用该代理
    if watch.get('proxy') and watch.get('proxy') in list(self.proxy_list.keys()):
        return watch.get('proxy')

    # 优先级 3：全局代理有效 → 使用全局代理
    system_proxy_id = self.data['settings']['requests'].get('proxy')
    if self.proxy_list.get(system_proxy_id):
        return system_proxy_id

    # 优先级 4：全局代理无效或不存在 → 取代理列表第一个
    if system_proxy_id is None or not self.proxy_list.get(system_proxy_id):
        first_default = list(self.proxy_list)[0]
        return first_default
```

#### 决策流程完整矩阵

| 场景 | Watch.proxy | 全局 requests.proxy | 代理列表 | 最终返回 |
|------|-------------|-------------------|---------|---------|
| 无任何代理配置 | - | - | `None` | `None`（无代理） |
| Watch 选 no-proxy（且功能启用） | `"no-proxy"` | 任意 | 非空 | `None`（强制无代理） |
| Watch 指定有效代理 | `"my-proxy"` | 任意 | 包含 `"my-proxy"` | `"my-proxy"` |
| Watch 指定无效代理 | `"invalid-key"` | 有效 | 非空 | 全局代理 key |
| Watch 指定无效代理 | `"invalid-key"` | 无效 / None | 非空 | 代理列表第一个 key |
| Watch 未设置（None） | `None` | 有效 | 非空 | 全局代理 key |
| Watch 未设置（None） | `None` | 无效 / None | 非空 | 代理列表第一个 key |

**关键认知**：
- Watch 级 `proxy=None` 并非"无代理"，而是"使用全局默认代理"
- 只有显式设置 `proxy="no-proxy"` 才能真正关闭代理（当环境变量 `ENABLE_NO_PROXY_OPTION=True` 时）
- 当全局代理 key 失效时，系统会**静默回退**到代理列表的第一条，而不是报错或无代理

#### extra_browser_ 场景的代理旁路

源码：[processors/base.py#L176-L183](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py#L176-L183)

```python
if not prefer_fetch_backend.startswith('extra_browser_'):
    proxy_url = self.datastore.proxy_list.get(preferred_proxy_id).get('url')
else:
    logger.debug("Skipping adding proxy data when custom Browser endpoint is specified. ")
```

当使用自定义浏览器端点（`extra_browser_*`）时，即使选定了代理，也会被**主动跳过**，交由浏览器端点自行处理代理配置。

---

### 6.2 通知字段空值语义—— 空列表/空字符串会触发继承

> **`_check_cascading_vars()` 中的判断条件 `if v and ...` 使用 Python 的 truthy 语义，空列表 `[]`、空字符串 `''`、None 均被视为 falsy，从而触发向下一级（Tag→Global→内置默认）继承。**

#### 源码判断逻辑

源码：[notification_service.py#L29-L44](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py#L29-L44)

```python
v = watch.get(var_name)
if v and not watch.get('notification_muted'):   # ← "v" 必须 truthy
    # Watch 级返回
    return v

tags = datastore.get_all_tags_for_watch(...)
for tag_uuid, tag in tags.items():
    v = tag.get(var_name)
    if v and not tag.get('notification_muted'):  # ← "v" 必须 truthy
        return v

if datastore.data['settings']['application'].get(var_name):  # ← 全局也必须 truthy
    return datastore.data['settings']['application'].get(var_name)
```

**真值表**（Python truthy 判断）：

| 值 | truthy/falsy | 行为 |
|---|:---:|------|
| `['json://localhost/']` | truthy | ✅ 使用该值，停止级联 |
| `"Change detected"` | truthy | ✅ 使用该值，停止级联 |
| `[]`（空列表） | falsy | ❌ 跳过，继续向下查找 |
| `""`（空字符串） | falsy | ❌ 跳过，继续向下查找 |
| `None` | falsy | ❌ 跳过，继续向下查找 |
| `0` / `False` | falsy | ❌ 跳过，继续向下查找 |

#### 各字段空值行为矩阵

| 字段 | Watch 空值 → | Tag 空值 → | Global 空值 → | 最终回退到 |
|------|:---:|:---:|:---:|---------|
| `notification_urls` | `[]` → 查 Tag | `[]` → 查 Global | `[]` → 内置 | `None`（无通知） |
| `notification_title` | `""` → 查 Tag | `""` → 查 Global | `""` → 内置 | `default_notification_title` |
| `notification_body` | `""` → 查 Tag | `""` → 查 Global | `""` → 内置 | `default_notification_body` |
| `notification_format` | 见 6.3 节 | 见 6.3 节 | 见 6.3 节 | `'System default'` → 内置 |

**关键认知**：
- 在 Watch 或 Tag 级别设置 **空 `notification_urls`** 并不能清空通知——它会继续继承 Tag 或全局的 URL
- 如果要在 Watch 级别"真正清空通知发送"，必须将 `notification_muted=True`，而不是将 `notification_urls` 设为 `[]`
- 同理，清空 `notification_title` 或 `notification_body` 会导致继承模板，而非发送空内容

---

### 6.3 `notification_format` 的 `'System default'` 标记—— 仅 Watch 层识别，Tag 层不识别

> **`USE_SYSTEM_DEFAULT_NOTIFICATION_FORMAT_FOR_WATCH` 字符串的特殊值处理仅发生在 Watch 层的 `_check_cascading_vars()` 判断中，Tag 层无此逻辑。**

#### 完整解析逻辑

源码：[notification_service.py#L29-L54](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py#L29-L54) + [handler.py#L328-L331](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification/handler.py#L328-L331)

```python
# _check_cascading_vars() Watch 层：
v = watch.get(var_name)
if v and not watch.get('notification_muted'):
    # ↓ 仅这里有 System default 特殊处理
    if var_name == 'notification_format' and v == USE_SYSTEM_DEFAULT_NOTIFICATION_FORMAT_FOR_WATCH:
        return datastore.data['settings']['application'].get('notification_format') or default_notification_format
    return v

# _check_cascading_vars() Tag 层：
for tag_uuid, tag in tags.items():
    v = tag.get(var_name)
    if v and not tag.get('notification_muted'):
        return v    # ← 没有 System default 判断！

# process_notification() handler 层兜底：
if requested_output_format == USE_SYSTEM_DEFAULT_NOTIFICATION_FORMAT_FOR_WATCH:
    requested_output_format = datastore.data['settings']['application'].get(
        'notification_format', default_notification_format
    )
```

#### 各层级行为对比

| 层级 | 值为 `'System default'` 时的行为 | 是否识别特殊值 |
|------|:---:|:---:|
| Watch | 立即返回全局 `notification_format` 或内置默认 | ✅ |
| Tag | 将字符串 `'System default'` 原样返回给 handler | ❌ |
| Global | 直接读值，无特殊处理 | N/A |
| Handler（兜底） | 若收到 `'System default'`，再解析一次全局 | ✅ |

#### 完整行为矩阵

| Watch.notification_format | Tag.notification_format | Global.notification_format | 最终格式 |
|:---:|:---:|:---:|:---:|
| `'System default'` | 任意 | `'HTML'` | `'HTML'`（Watch 层即解析） |
| `'System default'` | 任意 | 空 | `default_notification_format`（Watch 层回退） |
| 空/falsy | `'System default'` | `'HTML'` | `'System default'` → handler 兜底解析为 `'HTML'` |
| 空/falsy | `'System default'` | 空 | `'System default'` → handler 兜底解析为 `default_notification_format` |
| 空/falsy | `'HTML'` | 任意 | `'HTML'` |
| 空/falsy | 空/falsy | `'HTML'` | `'HTML'` |
| `'text'` | 任意 | 任意 | `'text'` |

**潜在问题**：Tag 层若存储了 `'System default'` 字符串，它会被当作普通格式名透传到 handler，由 handler 兜底再次解析。当前实现虽可工作（两层兜底），但逻辑存在冗余，Tag 层行为与 Watch 层不一致。

---

### 6.4 `fetch_backend` 的多场景解析链—— PDF 强制、extra_browser 重定向、界面展示不同

> **`fetch_backend` 字段并非单一解析链。根据调用场景不同（实际检查、UI 展示、浏览器步骤、能力检测），存在 4 条不同的解析路径。**

#### 场景一：实际内容抓取（`processor.base.call_browser()`）

源码：[processors/base.py#L132-L174](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py#L132-L174)

```python
# Step 1: 取 Watch 原始值（无 is_pdf 判断！）
prefer_fetch_backend = self.watch.get('fetch_backend', 'system')

# Step 2: system → 全局
if not prefer_fetch_backend or prefer_fetch_backend == 'system':
    prefer_fetch_backend = self.datastore.data['settings']['application'].get('fetch_backend')

# Step 3: extra_browser_* → 重定向为 html_webdriver + 自定义 URL
if prefer_fetch_backend.startswith('extra_browser_'):
    (t, key) = prefer_fetch_backend.split('extra_browser_')
    connection = list(filter(lambda s: s['browser_name'] == key,
        self.datastore.data['settings']['requests'].get('extra_browsers', [])))
    if connection:
        prefer_fetch_backend = 'html_webdriver'   # ← 替换为实际 fetcher 类名
        custom_browser_connection_url = connection[0].get('browser_connection_url')

# Step 4: PDF 强制 html_requests（覆盖前面所有决定）
if self.watch.is_pdf:
    prefer_fetch_backend = "html_requests"

# Step 5: 类不存在 → 回退 html_requests
if hasattr(content_fetchers, prefer_fetch_backend):
    fetcher_obj = getattr(content_fetchers, prefer_fetch_backend)
else:
    fetcher_obj = getattr(content_fetchers, "html_requests")

# Step 6: browser_steps 强制 playwright（临时 hack）
if prefer_fetch_backend == 'html_webdriver' and self.watch.has_browser_steps:
    fetcher_obj = playwright_fetcher   # ← 替换类，不改变 prefer_fetch_backend 字符串
```

**抓取场景完整决策顺序**：

```
Watch.fetch_backend
  → 'system' 时替换为全局 fetch_backend
  → 若前缀 extra_browser_*，替换为 html_webdriver + 自定义连接 URL
  → 若 is_pdf，强制为 html_requests
  → 类不存在，回退 html_requests
  → 若 html_webdriver + 有 browser_steps，运行时切换为 playwright fetcher
```

#### 场景二：Watch 属性解析（`Watch.get_fetch_backend`）

源码：[model/Watch.py#L357-L389](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/Watch.py#L357-L389)

```python
@property
def get_fetch_backend(self):
    if self.is_pdf:
        return 'html_requests'      # ← PDF 直接返回 html_requests
    return self.get('fetch_backend') # ← 其余情况原样返回，不解析 'system'
```

**关键差异**：此属性**不解析 `'system'` 值**——当 Watch 使用默认配置时，它返回字符串 `'system'` 而非实际的 fetcher 名。解析留给调用方。

#### 场景三：监控列表界面展示（watch-overview.html）

源码：[watch-overview.html#L315](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/watchlist/templates/watch-overview.html#L315)

```html
{%- set effective_fetcher = watch.get_fetch_backend
    if watch.get_fetch_backend != "system"
    else system_default_fetcher -%}
```

- 先用 `get_fetch_backend`（会处理 PDF→html_requests）
- 若结果是 `"system"`，用 `system_default_fetcher`（全局 fetch_backend）替换
- 然后据此显示对应的状态图标

#### 场景四：浏览器步骤会话启动（browser_steps）

源码：[blueprint/browser_steps/__init__.py#L229-L231](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/browser_steps/__init__.py#L229-L231)

```python
fetcher_name = watch.get_fetch_backend or 'system'
if fetcher_name == 'system':
    fetcher_name = datastore.data['settings']['application'].get('fetch_backend', 'html_requests')
```

- 走 `get_fetch_backend`（PDF→html_requests）
- 若为 `'system'` 再解析全局
- 不涉及 extra_browser_* 和 browser_steps hack（因为已经在 browser_steps 模块内）

#### 场景五：能力检测（`Watch.fetcher_supports_screenshots`）

源码：[model/Watch.py#L392-L408](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/Watch.py#L392-L408)

```python
fetcher_name = self.get_fetch_backend  # PDF→html_requests，其余原样
if not fetcher_name or fetcher_name == 'system':
    fetcher_name = self._datastore['settings']['application'].get('fetch_backend', 'html_requests')

fetcher_class = getattr(content_fetchers, fetcher_name, None)
if fetcher_class is None:
    return False
return bool(getattr(fetcher_class, 'supports_screenshots', False))
```

**注意**：这里**没有处理 extra_browser_* 前缀**——如果 Watch 的 fetch_backend 是 `extra_browser_MyBrowser`，`content_fetchers` 中找不到同名类，`fetcher_class` 为 `None`，能力检测会返回 `False`，即使该自定义浏览器实际支持截图。这是一个潜在的逻辑缺陷。

#### 四场景对比总结

| 解析场景 | 处理 PDF？ | 解析 system→全局？ | 解析 extra_browser_*？ | 无效类回退？ | browser_steps hack？ |
|---------|:---:|:---:|:---:|:---:|:---:|
| 实际抓取 | ✅ Step 4 | ✅ Step 2 | ✅ Step 3 | ✅ Step 5 | ✅ Step 6 |
| `Watch.get_fetch_backend` | ✅ | ❌ 返回 `'system'` | ❌ 原样返回 | ❌ | ❌ |
| 界面展示 | ✅ | ✅ Jinja 模板里 | ❌ 原样 | ❌ | ❌ |
| 浏览器步骤 | ✅ | ✅ | ❌ 已在模块内 | ❌ | ❌ |
| 能力检测 | ✅ | ✅ | ❌ 会误判为 False | N/A | ❌ |

---

## 七、此前版本的修正清单

| 序号 | 原描述 | 修正后 | 涉及源码 |
|------|--------|--------|---------|
| 1 | `use_page_title_in_list` 为"三态布尔 Watch→Global 级联回退"，全局路径 `settings.application.use_page_title_in_list` | 全局路径为 `settings.application.ui.use_page_title_in_list`；运行时为 **OR 逻辑**（全局开时 Watch 无法关闭） | [watch-overview.html#L244-L295](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/watchlist/templates/watch-overview.html#L244-L295)、[App.py#L78](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/App.py#L78) |
| 2 | `subtractive_selectors` 合并顺序为"Watch + Tag + Global"，使用去重 | 合并顺序为 **Tag → Watch → Global**，**不去重**（与其他过滤器不同） | [processor.py#L79-L86](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py#L79-L86) |
| 3 | `time_schedule_limit` 未与 `time_between_check` 关联说明 | `time_schedule_limit` 与 `time_between_check` 共用 `time_between_check_use_default` 开关；全局存储在 `settings.requests`（非 `settings.application`） | [flask_app.py#L1189-L1197](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/flask_app.py#L1189-L1197) |
| 4 | `scheduler_timezone_default` 为"仅全局配置，不可本地覆盖" | 全局 `scheduler_timezone_default` 自身确实只在全局，但 Watch 可通过 `time_schedule_limit.timezone` 子字段间接覆盖；解析链：schedule.timezone → scheduler_timezone_default → env TZ → UTC | [flask_app.py#L1199](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/flask_app.py#L1199)、[time_handler.py#L96-L98](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/time_handler.py#L96-L98) |
| 5 | `proxy` 仅简单描述为"None→Global 回退" | 实际为四级决策链：no-proxy→Watch 有效 key→全局有效 key→代理列表首项；且 extra_browser_* 场景会绕过代理 | [store/__init__.py#L855-L884](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py#L855-L884) |
| 6 | 通知字段空值未说明 | `notification_urls` 空列表、`notification_title/body` 空字符串均为 falsy，触发继续继承而非清空；清空通知需设 `notification_muted=True` | [notification_service.py#L29-L44](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py#L29-L44) |
| 7 | `notification_format` 的 System default 仅说明 Watch 层 | 仅 Watch 层识别 `'System default'`，Tag 层不识别，会由 handler 兜底二次解析；两者行为不一致 | [notification_service.py#L31-L32](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py#L31-L32)、[handler.py#L328-L331](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification/handler.py#L328-L331) |
| 8 | `fetch_backend` 仅说明基础解析链 | 存在 5 种不同的解析链（实际抓取/属性/UI/browser_steps/能力检测），对 PDF、extra_browser_*、browser_steps 的处理各有不同；能力检测对 extra_browser_* 存在潜在缺陷 | [processors/base.py#L132-L174](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py#L132-L174)、[Watch.py#L357-L408](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/Watch.py#L357-L408) |

---

## 八、关键源码参考

| 模块 | 文件路径 | 核心功能 |
|------|---------|---------|
| 数据模型 | [model/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/__init__.py) | `watch_base` 默认值定义 |
| Watch 模型 | [model/Watch.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/Watch.py) | Watch 领域模型、get_fetch_backend、能力检测 |
| 全局配置 | [model/App.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/App.py) | `base_config` 全局结构 |
| 数据存储 | [store/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py) | `add_watch()`、`get_preferred_proxy_for_watch()` 代理决策 |
| 表单定义 | [forms.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/forms.py) | 共享字段、三态布尔、ScheduleLimitForm |
| 通知服务 | [notification_service.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py) | `_check_cascading_vars()` 三级级联、空值真值判断 |
| 通知处理 | [notification/handler.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification/handler.py) | `process_notification()` format 兜底解析 |
| 处理器基类 | [processors/base.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py) | fetch_backend 完整解析链、extra_browser_* 重定向、代理旁路 |
| 文本处理器 | [processors/text_json_diff/processor.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py) | `FilterConfig` 过滤器合并、subtractive_selectors 专用属性 |
| 通知队列 | [queue_handlers.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/queue_handlers.py) | `all_muted` 全局静默门控 |
| 调度器 | [flask_app.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/flask_app.py) | 调度循环、时间窗口判断、时区解析 |
| 时间处理 | [time_handler.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/time_handler.py) | `is_within_schedule()` 时区解析 |
| 编辑页面 | [blueprint/ui/edit.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/ui/edit.py) | 表单保存、时区回退 |
| 监控列表模板 | [watch-overview.html](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/watchlist/templates/watch-overview.html) | `use_page_title_in_list` OR 逻辑、fetch_backend UI 展示 |
| 浏览器步骤 | [blueprint/browser_steps/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/browser_steps/__init__.py) | fetcher_name 解析链 |
| 三态控件 | [widgets/ternary_boolean.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/widgets/ternary_boolean.py) | `TernaryNoneBooleanField` |
