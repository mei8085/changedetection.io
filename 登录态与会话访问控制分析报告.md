# 登录态与会话信息访问控制机制分析报告

## 一、项目概述

本项目是 `changedetection.io`，一个基于 Flask 的网页变更检测应用。系统采用**双层认证拦截机制**来保护界面页面的访问，结合登录态（Authentication Status）与会话信息（Session Information）共同决定访问放行。

---

## 二、核心技术栈

| 技术组件 | 用途 | 相关文件 |
|---------|------|---------|
| Flask-Login | 用户认证与会话管理 | [flask_app.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py) |
| Flask Session | 用户偏好存储（语言等） | [flask_app.py#L125-L131](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L125-L131) |
| PBKDF2-HMAC-SHA256 | 密码哈希验证 | [flask_app.py#L437-L459](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L437-L459) |
| 自定义装饰器 | 路由级认证保护 | [auth_decorator.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/auth_decorator.py) |

---

## 三、登录态与会话信息的组成

### 3.1 登录态（Authentication Status）

**核心判断依据**：`flask_login.current_user.is_authenticated`

登录态由 Flask-Login 管理，通过以下方式建立和维护：

1. **用户登录**：在 [login](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L663-L696) 路由中验证密码成功后调用：
   ```python
   flask_login.login_user(user, remember=True)
   ```

2. **密码验证**：[User.check_password](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L437-L459) 方法使用 PBKDF2-HMAC-SHA256 算法：
   - 盐值长度：32 字节
   - 迭代次数：100,000 次
   - 密码存储位置：环境变量 `SALTED_PASS` 或 datastore 配置

3. **用户类**：[User](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L420-L461) 类继承自 `flask_login.UserMixin`，提供认证相关接口。

### 3.2 会话信息（Session Information）

系统使用**两种独立的 Cookie**：

| Cookie 类型 | 用途 | 配置 |
|------------|------|------|
| **Flask Session Cookie** | 存储用户偏好（`locale` 语言设置） | 有效期约 10 年（`PERMANENT_SESSION_LIFETIME`） |
| **Flask-Login Remember-Me Cookie** | 存储认证状态 | `remember=True` 时创建，长期有效 |

**会话配置** [flask_app.py#L125-L131](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L125-L131)：
```python
app.config['PERMANENT_SESSION_LIFETIME'] = timedelta(days=3650)  # ~10年
```

### 3.3 共同决定访问放行的逻辑

访问放行需要同时满足以下条件：

1. **密码启用检查**：系统是否配置了密码保护
   ```python
   has_password_enabled = datastore.data['settings']['application'].get('password') 
                          or os.getenv("SALTED_PASS", False)
   ```

2. **登录态检查**：用户是否已通过认证
   ```python
   current_user.is_authenticated
   ```

3. **豁免规则检查**：当前请求是否在豁免列表中

---

## 四、未授权访问的拦截层级

系统采用**双层拦截机制**，未授权访问会在两个层级被拦截：

### 4.1 第一层：全局 `before_request` 钩子（最早拦截）

**位置**：[flask_app.py#L526-L560](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L526-L560)

**执行时机**：每个请求到达路由处理函数**之前**

**拦截逻辑**：
```python
@app.before_request
def check_authentication():
    has_password_enabled = datastore.data['settings']['application'].get('password') or os.getenv("SALTED_PASS", False)
    
    if has_password_enabled and not flask_login.current_user.is_authenticated:
        # 检查豁免路径
        if ...:  # 各种豁免条件
            return None  # 放行
        else:
            return login_manager.unauthorized()  # 拦截，重定向到登录页
```

**豁免路径**（允许未授权访问）：

| 路径类型 | 豁免条件 | 说明 |
|---------|---------|------|
| 静态资源 | `static_content`, `static_flags` | CSS、JS、图片等 |
| 语言设置 | `set_language` | 登录页也需要切换语言 |
| 登录相关 | `'login' in request.endpoint` | 登录页面本身 |
| 共享差异 | `SHARED_DIFF_READ_ONLY_ENDPOINTS` + `shared_diff_access` 配置 | 公开的差异页面 |
| 豁免方法 | `flask_login.config.EXEMPT_METHODS` | 通常是 OPTIONS 请求 |
| RSS Feed | `'rss.feed' in request.endpoint` | RSS 通过 token 单独认证 |
| Socket.IO | `/socket.io/` | 实时通信，单独处理 |
| API 路径 | `/api/` | API 通过 `x-api-key` 单独认证 |

### 4.2 第二层：路由装饰器 `login_optionally_required`（二次校验）

**位置**：[auth_decorator.py#L16-L42](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/auth_decorator.py#L16-L42)

**应用方式**：作为装饰器应用于所有需要保护的路由：
```python
@watchlist_blueprint.route("/", methods=['GET'])
@login_optionally_required
def index():
    ...
```

**校验逻辑**：
1. 检查 `shared_diff_access` 配置下的只读端点豁免
2. 检查豁免 HTTP 方法
3. 检查 `LOGIN_DISABLED` 配置
4. 检查密码启用状态与登录态
5. 未通过则调用 `current_app.login_manager.unauthorized()`

**重要设计约束**：装饰器顺序至关重要，`@route` 必须在最外层（上方），`@login_optionally_required` 必须在内层（下方）。项目通过静态测试 [test_auth_decorator_order.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/tests/unit/test_auth_decorator_order.py) 强制保证这一点，防止认证被绕过。

---

## 五、其他认证机制

### 5.1 API 认证

**位置**：[api/auth.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/api/auth.py)

- 使用 `check_token` 装饰器
- 通过 `x-api-key` 请求头验证
- 独立于 Web 界面的会话认证

### 5.2 敏感静态资源认证

**位置**：[flask_app.py#L740-L852](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L740-L852)

以下静态资源组有额外的认证检查：
- `screenshot` - 监控页面截图
- `favicon` - 网站图标
- `visual_selector_data` - 可视化选择器数据

### 5.3 RSS Token 认证

RSS Feed 通过 URL 参数中的 `token` 进行认证，独立于会话机制。

---

## 六、访问控制流程图

```
请求到达
   │
   ▼
┌─────────────────────────────────┐
│  第一层: before_request 钩子    │  ◄── 最早拦截点
│  [flask_app.py#L526-L560]       │
└─────────┬───────────────────────┘
          │
          ├─ 密码未启用? ──► 放行
          │
          ├─ 已登录? ──► 放行
          │
          ├─ 豁免路径? ──► 放行
          │
          ▼
┌─────────────────────────────────┐
│  拦截: 重定向到登录页           │
│  login_manager.unauthorized()   │
└─────────────────────────────────┘
          │（如果配置允许通过）
          ▼
┌─────────────────────────────────┐
│  第二层: login_optionally_required │
│  装饰器 [auth_decorator.py]     │  ◄── 二次校验
└─────────┬───────────────────────┘
          │
          ├─ 特殊豁免? ──► 放行
          │
          ├─ 密码未启用? ──► 放行
          │
          ├─ 已登录? ──► 放行
          │
          ▼
┌─────────────────────────────────┐
│  拦截: 重定向到登录页           │
└─────────────────────────────────┘
          │（通过所有检查）
          ▼
┌─────────────────────────────────┐
│  路由处理函数执行业务逻辑        │
└─────────────────────────────────┘
```

---

## 七、关键安全设计点

### 7.1 双重防护机制
全局 `before_request` + 路由装饰器的双重检查，即使某一层出现配置错误，另一层仍能提供保护。

### 7.2 精确端点匹配
共享差异端点使用精确的端点名称匹配（`frozenset`），而非前缀匹配，防止路径遍历绕过认证 [auth_decorator.py#L10-L14](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/auth_decorator.py#L10-L14)。

### 7.3 装饰器顺序静态检查
通过 AST 静态分析确保装饰器顺序正确，防止认证绕过 [test_auth_decorator_order.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/tests/unit/test_auth_decorator_order.py)。

### 7.4 密码安全存储
使用 PBKDF2-HMAC-SHA256 加盐哈希，而非明文存储。

---

## 八、相关文件清单

| 文件路径 | 功能描述 |
|---------|---------|
| [flask_app.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py) | 主应用文件，包含全局拦截、登录逻辑、User 类 |
| [auth_decorator.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/auth_decorator.py) | 路由级认证装饰器 |
| [api/auth.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/api/auth.py) | API Token 认证 |
| [blueprint/ui/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/blueprint/ui/__init__.py) | UI 蓝图路由，应用认证装饰器 |
| [blueprint/watchlist/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/blueprint/watchlist/__init__.py) | 监视列表路由，应用认证装饰器 |
| [tests/unit/test_auth_decorator_order.py](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/tests/unit/test_auth_decorator_order.py) | 装饰器顺序静态检查 |

---

## 九、总结

### 9.1 访问放行判定公式
```
允许访问 = (密码未启用) OR (已登录) OR (属于豁免路径)
```

### 9.2 拦截层级结论
未授权访问主要在**两个层级**被拦截：

1. **最高优先级**：`before_request` 全局钩子 [flask_app.py#L526-L560](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/flask_app.py#L526-L560)
   - 这是最早的拦截点
   - 覆盖所有请求
   - 性能最优（在路由匹配前执行）

2. **第二道防线**：`login_optionally_required` 装饰器 [auth_decorator.py#L16-L42](file:///d:/fz/0601-1/solo-dogfeeding/code/36-changedetection.io/changedetectionio/auth_decorator.py#L16-L42)
   - 路由级别的二次校验
   - 提供更细粒度的控制
   - 作为全局拦截的补充和备份

### 9.3 设计亮点
- **分层防御**：两层拦截机制提供冗余保护
- **按需豁免**：精确的豁免规则确保必要功能正常工作
- **安全审计**：静态测试防止配置错误导致的认证绕过
- **密码学安全**：使用标准算法安全存储密码
