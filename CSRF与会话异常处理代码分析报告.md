# CSRF校验失败提示链路与会话异常文案分析报告

## 一、分析背景

本报告从概念架构层面解析 Web 应用中跨站请求伪造（CSRF）校验失败时的一次性提示信息传递链路，以及各类会话异常场景下前端提示信息的来源与用户可见性。

---

## 二、CSRF防护的体系架构

### 2.1 防护层的分层设计

整个CSRF防护体系分为三个逻辑层次：

| 层次 | 作用域 | 核心职能 |
|------|--------|----------|
| 服务端中间件层 | 全部非API路由的POST/PUT/DELETE/PATCH请求 | 请求到达业务逻辑前执行令牌校验，校验失败直接终止请求 |
| 令牌下发层 | 页面渲染阶段 | 生成令牌并嵌入到页面中，供后续请求携带 |
| 客户端请求层 | 浏览器端所有非幂等异步请求 | 自动向请求中注入令牌，确保请求合法性 |

**重要前提**：CSRF校验仅对**写操作请求**（POST/PUT/DELETE/PATCH等非幂等方法）生效。GET请求本身被视为安全的只读操作，不触发CSRF校验。

### 2.2 令牌下发与携带机制

令牌通过两种并行通道下发：全局脚本变量（供异步请求使用）和表单隐藏字段（供原生表单提交使用）。客户端通过全局请求拦截器自动将令牌作为自定义HTTP请求头注入到非幂等请求中。

---

## 三、CSRF校验失败的一次性提示链路

### 3.1 校验失败的服务端行为

CSRF校验发生在服务端中间件层，早于任何业务逻辑代码的执行。当校验失败时，框架的默认行为是：直接向客户端返回 **400 Bad Request** 状态码，不使用框架的一次性消息机制，不生成重定向。本应用未注册自定义的校验失败处理器。

### 3.2 客户端提示链路

```
触发条件层 → 令牌失配层 → 服务端校验层 → 客户端识别层 → 用户提示层
```

客户端对CSRF校验失败的最终提示方式是浏览器原生模态弹窗（alert），文案为：

> *"There was a problem processing the request, please reload the page."*

该文案仅英文，未被纳入多语言翻译体系。

### 3.3 提示链路的关键特性

| 特性维度 | 具体表现 |
|---------|---------|
| 适用请求类型 | 仅POST等写操作请求；GET请求不触发CSRF校验 |
| 提示方式 | 浏览器原生模态弹窗（阻塞式） |
| 语言支持 | 仅有英文，无国际化翻译 |
| 消息传递载体 | HTTP状态码，非Cookie/非响应体JSON |

---

## 四、浏览器操作流程的用户提示精简梳理

### jQuery AJAX回调执行规则（前置知识）

本节分析依赖一个关键的jQuery行为：**当请求返回非2xx状态码时，`statusCode`映射处理器与`fail`回调都会执行**。`statusCode`处理器先执行，`fail`回调后执行，两者不存在互斥关系。

---

### 4.1 阶段一：会话启动（GET请求）

**请求性质**：GET请求，**不涉及CSRF校验**。启动阶段的400错误不应归因为CSRF校验失败。

#### 前端预留处理器

| 处理器 | 触发条件 | 弹窗内容 |
|-------|---------|---------|
| `statusCode[400]` | 服务端返回400 | alert: "There was a problem processing the request, please reload the page." |
| `statusCode[401]` | 服务端返回401 | alert: 原始响应文本 |
| `.fail()` | 请求失败（非2xx均触发） | alert: "There was an error communicating with the server." |

#### 后端实际返回情况

| 触发条件 | 状态码 | 响应体内容 |
|---------|-------|-----------|
| 缺少Watch UUID参数 | 500 | "No Watch UUID specified" |
| 远端浏览器服务拒绝连接（ECONNREFUSED） | 401 | "Unable to start the Playwright Browser session, is sockpuppetbrowser running? Network configuration is OK?" |
| 其他启动异常 | 401 | 异常字符串原文 |
| 启动成功 | 200 | JSON: `{browsersteps_session_id: "..."}` |

#### ⚠️ 启动失败时的双重弹窗问题

根据jQuery回调执行规则，当启动请求返回400或401时：

- **400场景**：`statusCode[400]` 弹出alert → 用户关闭后 → `.fail()` 再弹出alert → **用户看到两次连续弹窗**
- **401场景**：`statusCode[401]` 弹出alert（显示原始异常文本）→ 用户关闭后 → `.fail()` 再弹出alert → **用户看到两次连续弹窗**

这是由于前端代码中`statusCode`处理器和`.fail()`回调都注册了`alert`调用，但两者并不互斥。只有返回200时（进入`.done()`），`.fail()`才不会执行。

**注意**：500状态码不在`statusCode`映射中，因此不会触发双重弹窗——仅`.fail()`弹出一次。

#### 用户实际体验汇总

| 后端返回 | 弹窗1（statusCode处理器） | 弹窗2（fail回调） | 用户看到 |
|---------|------------------------|-----------------|---------|
| 200 | 无 | 无（走done回调） | 无弹窗 |
| 400 | "There was a problem processing the request..." | "There was an error communicating with the server." | **两次弹窗** |
| 401（如浏览器服务不可达） | "Unable to start the Playwright Browser session..." | "There was an error communicating with the server." | **两次弹窗** |
| 500（如缺少UUID） | 无映射 | "There was an error communicating with the server." | 一次弹窗 |

#### 前端代码注释修正

前端`statusCode[400]`处理器注释写有"More than likely the CSRF token was lost when the server restarted"，这是**误导性注释**。GET请求不触发CSRF校验，启动阶段的400与CSRF令牌无关。

---

### 4.2 阶段二：交互操作（POST请求）

**请求性质**：POST请求，**需要CSRF令牌校验**。全局请求拦截器自动将令牌作为请求头注入。

交互操作包含两类子请求，均使用同一个POST异步请求函数：
1. **首次导航请求**：启动成功后自动发出
2. **用户步骤请求**：用户点击"应用"按钮后发出

#### 前端预留处理器

| 处理器 | 触发条件 | 弹窗内容 | 附加动作 |
|-------|---------|---------|---------|
| `statusCode[400]` | 服务端返回400 | alert: "There was a problem processing the request, please reload the page." | 隐藏加载动画 |
| `statusCode[401]` | 服务端返回401 | alert: 原始响应文本 | 隐藏加载动画 |
| `.done()` | 服务端返回200 | 无弹窗 | 更新截图、元素数据、恢复UI |
| `.fail()` | 请求失败（非2xx均触发） | **无弹窗** | 检查响应体是否含"Browser session expired"关键字 → 含则灰化UI |

#### 后端实际返回情况

| 触发条件 | 状态码 | 响应体内容 |
|---------|-------|-----------|
| 未提供会话ID | 500 | "No browsersteps_session_id specified" |
| 会话ID不存在（已被清理） | 500 | "No session exists under that ID" |
| 操作执行异常（含底层会话超时） | 401 | 异常首行文本 |
| 截图/状态获取异常 | 401 | "Error fetching screenshot and element data - {异常文本}" |
| 操作成功 | 200 | JSON: `{screenshot, xpath_data, session_age_start, browser_time_remaining}` |

#### CSRF校验失败在此阶段的表现

CSRF令牌失效时，服务端中间件返回400状态码（在请求到达业务逻辑之前被拦截）。此时：
- `statusCode[400]` → 弹出alert（通用错误提示）
- `.fail()` → 检查过期关键字 → 不匹配 → 不灰化

**交互阶段不存在双重弹窗**，因为`.fail()`回调中没有`alert`调用（与启动阶段不同）。

---

### 4.3 阶段三：会话过期

#### 后端过期判定机制

每个会话实例维护独立的过期状态属性，判定条件：
1. 底层浏览器页面资源已被清理
2. 自创建时刻起超过最大存活时间（默认10分钟，可通过环境变量调整）

后台存在机会式清理流程——每当有会话相关操作到达时，遍历全部活跃会话，对过期会话执行资源回收。

#### 代码确认的过期提示路径

以下路径由代码逻辑直接确认可触发：

**路径A：预防性倒计时（过期前）**

- 信息来源：前端本地计时器，初始值硬编码为500秒
- 与后端真实过期时间（默认600秒）存在100秒偏差
- 用户提示："N seconds remaining in session"（英文数字）

**路径B：401 + 含过期关键字 → 先弹窗再灰化**

- 触发条件：POST交互请求时底层操作异常，服务端以401返回异常首行文本，且响应体包含"Browser session expired"关键字
- 执行顺序：
  1. `statusCode[401]` → alert弹窗显示异常首行（技术化文本）
  2. 用户关闭alert后 → `.fail()`执行 → 匹配过期关键字 → UI灰化（不透明度0.3 + 移除鼠标事件）
- **代码确认度**：✅ 已确认——`statusCode[401]`与`.fail()`都会执行，且`.fail()`中明确检查过期关键字

**路径C：会话已被清理 → 静默失败**

- 触发条件：POST交互请求时，会话ID已不在内存映射表中
- 服务端返回500 + 错误文本
- 前端：`statusCode`无500映射 → 仅`.fail()`执行 → 不匹配过期关键字 → 不灰化、不弹窗、仅打印控制台日志
- **代码确认度**：✅ 已确认——500不在statusCode映射中，`.fail()`中无alert调用

#### ⚠️ 理论可能但未由代码结构化保证的路径

**路径D：非400/401状态码 + 含过期关键字 → 仅灰化**

- 触发条件：假设某种场景下服务端返回非400非401状态码，且响应体包含"Browser session expired"
- 此时`statusCode`映射不命中 → 仅`.fail()`执行 → 匹配关键字 → 仅灰化
- **代码确认度**：⚠️ 前端代码路径存在，但**后端当前不会主动返回此组合**——后端仅返回200/400（CSRF中间件）/401（业务异常）/500（会话不存在），不存在返回其他状态码并携带过期关键字的逻辑路径
- 因此"仅灰化无弹窗"在当前后端实现下**不会独立出现**，它仅作为401路径中alert关闭后的后续副作用存在

---

### 4.4 浏览器操作流程提示矩阵（仅代码已验证分支）

| 阶段 | 方法 | 后端返回 | 前端statusCode处理器 | 前端fail回调 | 用户最终体验 |
|-----|------|---------|--------------------|-------------|-------------| 
| 启动 | GET | 200 | — | 不执行（走done） | 无提示，自动进入交互 |
| 启动 | GET | 400 | alert: "problem...reload" | alert: "error communicating" | **两次连续弹窗** |
| 启动 | GET | 401（浏览器服务不可达） | alert: 原始异常文本 | alert: "error communicating" | **两次连续弹窗** |
| 启动 | GET | 500（缺参数等） | 无映射 | alert: "error communicating" | 一次弹窗 |
| 交互 | POST | 200 | — | 不执行（走done） | 无提示，更新截图 |
| 交互 | POST | 400（CSRF失效） | alert: "problem...reload" | 检查关键字→不匹配 | 一次弹窗，不灰化 |
| 交互 | POST | 401（异常，不含过期关键字） | alert: 异常首行 | 检查关键字→不匹配 | 一次弹窗，不灰化 |
| 交互 | POST | 401（异常，含过期关键字） | alert: 异常首行 | 检查关键字→匹配→灰化 | **先弹窗，关闭后灰化** |
| 交互 | POST | 500（会话已清理） | 无映射 | 检查关键字→不匹配 | **静默失败，无弹窗无灰化** |
| 过期前 | — | — | — | — | 倒计时："N seconds remaining" |

---

## 五、会话异常场景的提示信息溯源

本系统存在三种不同语义的"会话"概念，各自的异常提示链路截然不同：

| 会话类型 | 生命周期管理方 | 典型异常场景 |
|---------|---------------|-------------|
| 用户认证会话 | 登录管理框架 | 未登录访问受限资源、密码错误 |
| Flask会话Cookie | Web框架会话机制 | 客户端禁用Cookie导致会话无法建立 |
| 浏览器自动化会话 | 独立的浏览器步骤子系统 | 会话超时、会话被后台清理 |

---

### 5.1 场景一：用户认证异常

#### 5.1.1 未登录访问受限页面

在全局前置拦截阶段检测到用户未认证，服务端将消息写入一次性消息存储区并重定向至登录页。中文翻译：**"需要登录，请先登录。"**，类别为`error`，页面顶部红色区域常驻显示。

**用户可见性**：✅ 明确可见。

#### 5.1.2 登录密码错误

密码校验失败后写入一次性消息存储区，POST-Redirect-GET重定向回登录页。中文翻译：**"密码错误"**，类别为`error`，页面顶部红色区域常驻显示。

**用户可见性**：✅ 明确可见。

#### 5.1.3 已登录状态访问登录页

检测到已认证，写入信息性消息并重定向至首页。中文翻译：**"已登录"**，非error类别，转换为Toast通知6秒后消失。

**用户可见性**：✅ 可见但短暂。

---

### 5.2 场景二：Flask会话Cookie缺失

用户切换语言时检测到客户端无Cookie，写入错误消息但**未经过翻译函数**，直接使用英文原文："Cannot set language without session cookie"，类别为`error`，页面顶部红色区域常驻。

**用户可见性**：✅ 可见但为英文。

---

### 5.3 场景三：浏览器自动化会话过期

详见第四章分析。核心发现：

- **401 + 过期关键字**：先弹窗（技术化异常文本）再灰化——用户需在困惑弹窗和突然灰化间建立因果联系
- **500（会话已清理）**：静默失败——用户可能误以为操作成功
- **倒计时**：数值与后端不一致（前端500秒 vs 后端600秒）

---

## 六、一次性消息系统的通用传递模型

### 6.1 消息生命周期

```
写入阶段（服务端调用消息入队函数）
    → 存储阶段（签名Cookie，跨302重定向传递）
    → 读取阶段（模板引擎消费，读后即焚）
    → 呈现阶段（error类常驻页面 / 其他类转Toast 6秒消失）
```

### 6.2 消息分类呈现规则

| 消息类别 | 呈现方式 | 持续时间 |
|---------|---------|---------|
| `success` | 转换为Toast | 6秒自动消失 |
| `info` / `message` / `notice` | 转换为Toast | 6秒自动消失 |
| `warning` | 转换为Toast | 6秒自动消失 |
| `error` / `danger` | 保留在页面固定区域 | 用户刷新或下一次跳转前 |

---

## 七、异常场景用户文案汇总表

| 异常场景 | 方法 | 用户实际看到的文案 | 语言 | 呈现方式 | 可见性 |
|---------|------|-------------------|------|---------|-------|
| CSRF令牌校验失败 | POST | "There was a problem processing the request, please reload the page." | 英文 | alert弹窗 | ⭐⭐⭐ |
| 浏览器启动-400 | GET | 先弹"problem...reload"，再弹"error communicating" | 英文 | **两次alert** | ⭐⭐ 双重弹窗 |
| 浏览器启动-401 | GET | 先弹原始异常文本，再弹"error communicating" | 英文 | **两次alert** | ⭐⭐ 双重弹窗 |
| 浏览器启动-500 | GET | "There was an error communicating with the server." | 英文 | alert弹窗 | ⭐⭐⭐ |
| 浏览器交互-401含过期关键字 | POST | 先弹异常首行，关闭后UI灰化 | 英文 | alert→灰化 | ⭐⭐ 先弹窗再灰化 |
| 浏览器交互-401不含过期关键字 | POST | 异常首行文本 | 英文 | alert弹窗 | ⭐⭐⭐ 技术化 |
| 浏览器交互-500会话已清理 | POST | 无提示 | — | 无 | ⭐ 静默失败 |
| 未登录访问受限页面 | * | "需要登录，请先登录。" | 中文 | 页面红色区域 | ⭐⭐⭐⭐⭐ |
| 登录密码错误 | POST | "密码错误" | 中文 | 页面红色区域 | ⭐⭐⭐⭐⭐ |
| 已登录访问登录页 | GET | "已登录" | 中文 | Toast 6秒 | ⭐⭐⭐⭐ |
| 无Cookie切换语言 | GET | "Cannot set language without session cookie" | 英文 | 页面红色区域 | ⭐⭐⭐ |

---

## 八、关键发现与设计洞察

### 8.1 两套独立的一次性提示体系

1. **框架级一次性消息系统**：基于签名Cookie，依赖页面重定向，支持多语言与分类呈现
2. **HTTP状态码驱动的前端弹窗系统**：基于状态码映射，文案硬编码，未接入国际化

### 8.2 启动阶段的双重弹窗缺陷

启动请求（GET）返回400或401时，由于`statusCode`处理器和`.fail()`回调都注册了`alert`且不互斥，用户会看到**两次连续弹窗**：
- 第一次：`statusCode`处理器的专用错误信息
- 第二次：`.fail()`回调的通用错误信息

这是明显的代码缺陷——两个处理器应互斥，或`.fail()`中应判断已知状态码后跳过弹窗。500状态码因不在`statusCode`映射中，反而只弹一次窗，行为不一致。

### 8.3 交互阶段不存在双重弹窗

交互请求的`.fail()`回调中没有`alert`调用，仅执行过期关键字检查和UI恢复操作。因此即使`statusCode[400]`或`statusCode[401]`触发了弹窗，`.fail()`也不会再弹窗。这避免了启动阶段的双重弹窗问题，但引入了另一问题——500状态码时完全静默。

### 8.4 "先弹窗再灰化"的两步反馈

当POST交互请求返回401且响应体包含过期关键字时：`statusCode[401]`先弹出异常首行文本，用户关闭后`.fail()`匹配关键字触发灰化。用户需在技术化弹窗和突然灰化的界面之间建立因果联系，认知负担大。

### 8.5 "仅灰化无弹窗"路径的理论性

前端代码中存在"非400/401状态码 + 含过期关键字 → 仅灰化"的路径，但**当前后端不会产生此状态码组合**。后端仅返回200/400/401/500四种状态码，其中500不含过期关键字。因此该路径在前端代码中是死路径，"仅灰化无弹窗"不会独立出现——它始终是401弹窗关闭后的后续副作用。

### 8.6 浏览器启动400错误不应归因CSRF

前端`statusCode[400]`处理器的注释"More than likely the CSRF token was lost"是误导性注释。GET请求不触发CSRF校验，启动阶段400应归因其他服务端异常。

### 8.7 国际化覆盖的盲点

以下关键文案未被纳入多语言翻译体系：
- CSRF校验失败提示（英文硬编码于前端脚本）
- 无Cookie时的语言设置失败（英文硬编码于后端）
- 浏览器自动化会话相关错误（异常透传）

---

**报告生成时间**：2026-06-11
