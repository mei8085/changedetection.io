# changedetection.io 全局配置默认值继承与本地覆盖机制分析

## 一、整体架构概述

changedetection.io 采用 **三级配置继承架构**（Chain Resolution）：

```
Watch 级别（监控条目）
    ↓ 未设置时向下查找
Tag 级别（标签/分组）
    ↓ 未设置时向下查找
Global Settings（全局配置）
    ↓ 未设置时使用
代码内置默认值
```

> **设计特点**：当前实现为 **手动散点式解析**，各处理器和业务逻辑中分别判断 `watch.get('field')` → Tag → Global 的优先级，存在一定技术债务。模型文档中有向 Pydantic `@computed_field` 自动解析迁移的计划。

---

## 二、新增监控条目时的默认值初始化流程

### 2.1 入口：`add_watch()` 方法

位置：[store/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py)

创建 Watch 时，通过 `Watch()` 构造函数实例化对象，其继承链为：

```
Watch (Watch.py)
    ↓ 继承
watch_base (model/__init__.py)
    ↓ 继承
dict
```

### 2.2 默认值定义位置

所有字段的默认值在 [model/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/__init__.py) 的 `watch_base.__init__()` 中通过 `self.update({...})` 统一设置。

### 2.3 关键设计：None = 使用全局默认

许多字段的初始值被设为 `None` 或特殊标记值，以此区分"用户未设置"与"用户显式设置"：

| 标记值 | 含义 |
|--------|------|
| `None` | 使用全局配置中的对应值 |
| `'system'` (fetch_backend) | 使用全局配置的 fetch_backend |
| `'System default'` (notification_format) | 使用全局配置的通知格式 |
| `time_between_check_use_default=True` | 时间间隔使用全局配置 |

> **源码注释**："Re #110, so then if this is set to None, we know to use the default value instead"

---

## 三、字段分类详解：继承方式与本地覆盖

### 3.1 三态布尔字段（TernaryNoneBooleanField）

这类字段使用 `TernaryNoneBooleanField` 控件，支持三种状态：

- **None**：使用全局默认值
- **True**：本地启用
- **False**：本地禁用

#### 适用字段：

| 字段名 | 说明 | 全局配置路径 |
|--------|------|-------------|
| `strip_ignored_lines` | 移除匹配忽略规则的行 | `settings.application.strip_ignored_lines` |
| `use_page_title_in_list` | 在监控列表中显示页面标题 | `settings.application.use_page_title_in_list` |
| `notification_muted` | 静默通知 | 通知级联逻辑中处理 |

#### 解析示例（[processors/text_json_diff/processor.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py)）：

```python
strip_ignored_lines = watch.get('strip_ignored_lines')
if strip_ignored_lines is None:
    strip_ignored_lines = self.datastore.data['settings']['application'].get('strip_ignored_lines')
```

---

### 3.2 通知系统字段（三级级联继承）

由 [notification_service.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py) 的 `_check_cascading_vars()` 函数统一处理。

#### 解析优先级：

```
Watch 本地值 → Tag 标签覆盖 → Global 全局值 → 代码内置默认
```

#### 适用字段：

| 字段名 | 说明 | 特殊规则 |
|--------|------|---------|
| `notification_urls` | 通知 Apprise URL 列表 | `notification_muted` 为 True 时跳过 Watch 层 |
| `notification_title` | 通知标题模板 | - |
| `notification_body` | 通知内容模板 | - |
| `notification_format` | 通知格式（Text/HTML 等） | Watch 值为 `'System default'` 时回退到全局 |

#### 核心解析逻辑：

```python
def _check_cascading_vars(datastore, var_name, watch):
    # 第1优先级：Watch 本地设置
    v = watch.get(var_name)
    if v and not watch.get('notification_muted'):
        if var_name == 'notification_format' and v == USE_SYSTEM_DEFAULT_NOTIFICATION_FORMAT_FOR_WATCH:
            return datastore.data['settings']['application'].get('notification_format') or default_notification_format
        return v
    
    # 第2优先级：查找关联 Tag 的覆盖
    for tag_uuid in watch.get('tags', []):
        tag = datastore.data['tags'].get(tag_uuid)
        if tag and tag.get('overrides_watch') and tag.get(var_name):
            return tag[var_name]
    
    # 第3优先级：全局配置
    return datastore.data['settings']['application'].get(var_name) or default_value
```

---

### 3.3 调度时间间隔字段

#### 字段组合：

- `time_between_check_use_default` (bool)：是否使用全局默认间隔
- `time_between_check` (dict)：本地自定义间隔 `{weeks, days, hours, minutes, seconds}`

#### 解析逻辑（[store/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py)）：

```python
# 当 time_between_check_use_default 为 True 时，
# 使用全局 settings.requests.time_between_check
# 否则使用 watch['time_between_check'] 的本地值
```

- **默认初始化时**：`time_between_check_use_default=True`，即新创建的 Watch 默认继承全局时间间隔
- **用户编辑表单勾选"使用默认"**：保持 True，不存储本地时间间隔值

---

### 3.4 内容获取后端（fetch_backend）

#### 字段：
- `fetch_backend`：`'system'` / `'html_requests'` / `'playwright_chromium'` 等

#### 解析逻辑（[processors/base.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py)）：

```python
prefer_fetch_backend = self.watch.get('fetch_backend', 'system')
if not prefer_fetch_backend or prefer_fetch_backend == 'system':
    prefer_fetch_backend = self.datastore.data['settings']['application'].get('fetch_backend')
```

- **默认初始化时**：`fetch_backend='system'`，使用全局配置
- **用户可本地覆盖**：选择具体的后端引擎

---

### 3.5 浏览器自动化相关字段

| 字段名 | 默认值 | 解析方式 |
|--------|--------|---------|
| `webdriver_delay` | `None` | Watch → Global（None 时用全局） |
| `webdriver_js_execute_code` | `None` | 仅本地设置，无全局继承 |

#### 解析示例（[processors/base.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py)）：

```python
webdriver_delay = self.watch.get('webdriver_delay')
if webdriver_delay is None:
    webdriver_delay = self.datastore.data['settings']['application'].get('webdriver_delay')
```

---

### 3.6 代理服务器（proxy）

#### 解析逻辑（[store/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py) → `get_preferred_proxy_for_watch()`）：

```
Watch['proxy'] → 全局 settings.requests.proxy
```

---

### 3.7 历史快照长度（history_snapshot_max_length）

#### 解析逻辑（[model/Watch.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/Watch.py) → `save_history_blob()`）：

```python
max_history = self.get('history_snapshot_max_length')
if max_history is None:
    max_history = self.get_global_setting('application', 'history_snapshot_max_length')
```

- **默认值**：`None` → 使用全局配置

---

### 3.8 文本过滤器（多层合并机制）

由 [processors/text_json_diff/processor.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py) 的 `FilterConfig._get_merged_rules()` 实现。

#### 合并策略：**列表追加 + 去重**

```python
def _get_merged_rules(self, attr, include_global=False):
    watch_rules = self.watch.get(attr, [])
    tag_rules = self.datastore.get_tag_overrides_for_watch(uuid=self.watch_uuid, attr=attr)
    rules = list(dict.fromkeys(watch_rules + tag_rules))  # Watch + Tag，保持顺序去重
    if include_global:
        global_rules = self.datastore.data['settings']['application'].get(f'global_{attr}', [])
        rules = list(dict.fromkeys(rules + global_rules))  # 追加全局规则
    return rules
```

#### 各字段合并层级：

| 字段名 | Watch 本地 | Tag 标签 | Global 全局 | 全局配置 key |
|--------|:----------:|:--------:|:-----------:|-------------|
| `include_filters` | ✅ | ✅ | ❌ | - |
| `subtractive_selectors` | ✅ | ✅ | ✅ | `global_subtractive_selectors` |
| `ignore_text` | ✅ | ✅ | ✅ | `global_ignore_text` |
| `extract_lines_containing` | ✅ | ✅ | ❌ | - |
| `extract_text` | ✅ | ✅ | ❌ | - |
| `trigger_text` | ✅ | ✅ | ❌ | - |
| `text_should_not_be_present` | ✅ | ✅ | ❌ | - |

> **注意**：过滤器是 **合并追加** 而非覆盖替换，本地规则与全局规则同时生效。

---

### 3.9 仅全局配置（不可本地覆盖）

以下字段仅读取全局配置，Watch 级别无对应字段：

| 字段名 | 全局配置路径 | 说明 |
|--------|-------------|------|
| `timeout` | `settings.requests.timeout` | HTTP 请求超时时间 |
| `empty_pages_are_a_change` | `settings.application.empty_pages_are_a_change` | 空页面是否视为变更 |
| `ignore_whitespace` | `settings.application.ignore_whitespace` | 比较时忽略空白（注意：另有本地字段 `trim_text_whitespace` 用于截取空白，两者不同） |
| `workers` | `settings.requests.workers` | 并发工作线程数 |
| `jitter_seconds` | `settings.requests.jitter_seconds` | 调度抖动秒数 |
| `default_ua` | `settings.requests.default_ua` | 默认 User-Agent |

---

## 四、表单层实现细节

位置：[forms.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/forms.py)

### 4.1 共享表单基类 `commonSettingsForm`

Watch 编辑表单和全局设置表单共享大量字段定义，保证字段命名一致。

### 4.2 `EnhancedFormField` 条件验证

`time_between_check` 使用 `EnhancedFormField` 包装，仅当 `time_between_check_use_default=False` 时才启用验证。

### 4.3 UI 层保存处理

位置：[blueprint/ui/edit.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/blueprint/ui/edit.py)

保存表单时，如果用户勾选"使用默认时间间隔"，则忽略本地提交的 `time_between_check` 值，保持 `time_between_check_use_default=True`。

---

## 五、字段总览表

### 可本地覆盖的字段（支持 Watch 级别设置）

| 字段名 | 默认值 | 继承方式 | 覆盖说明 |
|--------|--------|---------|---------|
| `fetch_backend` | `'system'` | Watch → Global | 'system' 表示用全局 |
| `time_between_check` | 全 None dict | 开关控制 | `use_default=True` 时用全局 |
| `time_between_check_use_default` | `True` | 仅本地 | 控制时间间隔继承开关 |
| `proxy` | `None` | Watch → Global | None 时用全局 |
| `webdriver_delay` | `None` | Watch → Global | None 时用全局 |
| `history_snapshot_max_length` | `None` | Watch → Global | None 时用全局 |
| `strip_ignored_lines` | `None` | Watch → Global | 三态布尔 |
| `use_page_title_in_list` | `None` | Watch → Global | 三态布尔 |
| `notification_muted` | `False` | 本地 + Tag 级联 | 影响通知是否发送 |
| `notification_urls` | `[]` | Watch → Tag → Global | 级联查找 |
| `notification_title` | `None` | Watch → Tag → Global | 级联查找 |
| `notification_body` | `None` | Watch → Tag → Global | 级联查找 |
| `notification_format` | `'System default'` | Watch → Tag → Global | 特殊值触发回退 |
| `include_filters` | `[]` | Watch + Tag 合并 | 列表追加合并 |
| `subtractive_selectors` | `[]` | Watch + Tag + Global 合并 | 列表追加合并 |
| `ignore_text` | `[]` | Watch + Tag + Global 合并 | 列表追加合并 |
| `extract_lines_containing` | `[]` | Watch + Tag 合并 | 列表追加合并 |
| `extract_text` | `[]` | Watch + Tag 合并 | 列表追加合并 |
| `trigger_text` | `[]` | Watch + Tag 合并 | 列表追加合并 |
| `text_should_not_be_present` | `[]` | Watch + Tag 合并 | 列表追加合并 |

### 仅全局配置的字段（不可本地覆盖）

| 字段名 | 全局配置路径 |
|--------|-------------|
| `timeout` | `settings.requests.timeout` |
| `workers` | `settings.requests.workers` |
| `jitter_seconds` | `settings.requests.jitter_seconds` |
| `default_ua` | `settings.requests.default_ua` |
| `ignore_whitespace` | `settings.application.ignore_whitespace` |
| `empty_pages_are_a_change` | `settings.application.empty_pages_are_a_change` |
| `scheduler_timezone_default` | `settings.application.scheduler_timezone_default` |

---

## 六、技术债务与未来改进

根据源码注释，当前实现存在以下待改进点：

1. **解析逻辑分散**：各处理器手动实现 Watch→Tag→Global 查找，缺乏统一入口
2. **Pydantic 迁移计划**：计划使用 `@computed_field` 实现自动解析，例如 `watch.resolved_fetch_backend`
3. **dict 继承问题**：`watch_base` 继承自 `dict` 违反封装原则，计划重构为 Pydantic `BaseModel`

---

## 七、关键源码参考

| 模块 | 文件路径 | 核心功能 |
|------|---------|---------|
| 数据模型 | [model/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/__init__.py) | `watch_base` 默认值定义 |
| Watch 模型 | [model/Watch.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/Watch.py) | Watch 领域模型 |
| 全局配置 | [model/App.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/model/App.py) | `base_config` 全局结构 |
| 数据存储 | [store/__init__.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/store/__init__.py) | `add_watch()`、代理解析 |
| 表单定义 | [forms.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/forms.py) | 共享字段、三态布尔字段 |
| 通知服务 | [notification_service.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/notification_service.py) | `_check_cascading_vars()` |
| 处理器基类 | [processors/base.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/base.py) | fetch_backend、delay 解析 |
| 文本处理器 | [processors/text_json_diff/processor.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/processors/text_json_diff/processor.py) | `FilterConfig` 过滤器合并 |
| 三态控件 | [widgets/ternary_boolean.py](file:///d:/fz/0601-1/solo-dogfeeding/code/7-changedetection.io/changedetectionio/widgets/ternary_boolean.py) | `TernaryNoneBooleanField` |
